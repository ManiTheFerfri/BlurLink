# V2 Release (M4) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cut the 0.3.0 release: hygiene closed, README honest, WPF removed, portable proven, tag pushed, public-mirror path documented.

**Architecture:** Shell-only edits plus docs plus packaging retarget (Desktop → Shell). No new projects, no new packages, no native changes except none. M1 skipped by user decision (recorded 2026-09-21 in the production ledger): (a)+(c) proven live join-only, (b) non-blocking, kit ceremony unnecessary.

**Tech Stack:** .NET 10 LTS (`net10.0-windows`), Avalonia 12.1.2, xUnit (v2 Core / v3 Shell), PowerShell 7 (CI; this machine has PS 5.1 only — replicate where noted), CMake/MSVC native (untouched).

**Spec:** `docs/superpowers/specs/2026-09-12-v2-design.md` §9 M4 (tagged release downloadable with hashes/notices; README rewritten around verified-vs-open; WPF removed), D10 (slim stays deliverable), D12 (`-Full`/`-Sfx` retired IF AOT delivers — it did not, so they stay; record why), Q6/Q7.

## Global Constraints

- .NET 10 LTS floor; Windows-only; no other OS work.
- Zero new runtime dependencies. No DI container, no MVVM toolkit, no webview.
- No payload rewriting; no telemetry/accounts/backend/listener weakening; metadata-only diagnostics; synthetic fixtures only.
- WPF may be touched ONLY to delete it (Task 4). Until then it stays frozen.
- Native helper: no change.
- Honesty rules: exit 2 never a pass; PENDING labeled never faked; no launches/screenshots/elevated runs claimed without evidence; README states verified status honestly (join-only proven on author's pair; host fallback retained; driver CI PENDING; elevated e2e assertions pending).
- Test baseline at plan time: 308 managed (Core 277 + Shell 31). Keep green; name every updated assertion in the commit message.
- No GitHub remote exists (origin is GitLab-only). Anything requiring GitHub (release attach, driver dispatch) is prepped + documented, trigger PENDING. Tags pushed to origin (GitLab) only with user approval at execution time (merge/push/tag are side effects — confirm before running).

---

## Rulings (binding; decided here so implementers never re-decide)

- **R1 — single-instance mutex KEEPS its name** (`Local\BlurLink.Desktop.SingleInstance`). Renaming orphans the guard during upgrade overlap (old WPF + new Shell co-installed would double-divert). Cost if wrong: duplicate forwarding for upgraders. Record in REPO.md history notes.
- **R2 — `-Full`/`-Sfx` STAY (D12 resolved: do not retire).** AOT failed on both shells (WPF NETSDK1168; Shell 16× IL2026/IL3050 at Core JSON sites), so the "AOT delivers" condition is false. Cost if wrong: no-runtime users lose their only working flavor.
- **R3 — public mirror method: restart public history from `a238ad0`** (first clean commit), never filter-repo surgery on this branch. Pre-scrub objects stay in the private clone only. Clean-clone banned-map grep is the proof gate. Executes at mirror time by the user, not in any task here.
- **R4 — IsVisible resolution: make it notifying** (not removed). Timer-gate + future visibility binding both work; deletion risks re-adding later.
- **R5 — PickLiveOrMax key: verify Mode lifecycle; if coordinator resets Mode on stop, keep as-is with a comment citing the reset site; else key off running state.** Implementer reads the code, picks by evidence, records which.

## Deliberately not in this plan

| Item | Where it lives |
|---|---|
| M1 evidence session | skipped by user decision 2026-09-21 (ledgered); (b) stays open-non-blocking |
| Payload-rewrite workstream | needs M1 gate evidence that was skipped — stays closed |
| Signing/MSIX/Store/winget/auto-update | spec non-goals |
| GitHub mirror creation + dispatches | user action at mirror time; this plan preps + documents only |
| AOT retry (source-gen JSON) | future workstream, never gates this release |

## File map

```
Task 1: tests/BlurLink.Shell.Tests/* (namespace), src/BlurLink.Shell/** (mojibake, double-Raise, IsVisible, Throwing dedup, preflight ifIdx, UiAudit msg, tray dispose, Dialog Esc, HasEmbeddedHelper+test, host expected-length unconditional→native? NO — native frozen; host expected-length assignment lives in managed main.cpp? NO — main.cpp is native. DROP that item: native frozen wins, park it), docs (stale .NET 8 sweep)
Task 2: no files (headed run, evidence in commit message)
Task 3: README.md (+ docs/user-guide.md touch-ups if the rewrite exposes staleness)
Task 4: BlurLink.sln, src/BlurLink.Desktop/** (delete), scripts/build-portable.ps1, scripts/package-release.ps1, src/BlurLink.Shell/BlurLink.Shell.csproj (Native embeds), docs/REPO.md (R1 note)
Task 5: dist/ (never staged), git tags (user-confirmed), docs/driver-ci-experiment.md untouched (still PENDING)
Task 6: docs/REPO.md (mirror runbook), docs/aot-spike.md (D12 record — append, don't rewrite)
```

---

# Stage A — Close the hygiene

## Task 1: Hygiene commit (deferred minors + M4 one-liners)

**Files:**
- Modify: `tests/BlurLink.Shell.Tests/DialogHeadlessTests.cs`, `NotificationCenterTests.cs` (namespace `BlurLink.Core.Tests` → `BlurLink.Shell.Tests`; grep ALL Shell.Tests files for the old namespace first — fix every hit, name each file)
- Modify: `src/BlurLink.Shell/Program.cs`, `App.axaml.cs`, `Themes/Tokens.axaml` + any view with `ΓÇö`-rendered comments (re-save UTF-8, em-dash `—`; verify with byte read `E2 80 94`, not by eye)
- Modify: `src/BlurLink.Shell/ViewModels/DiagnosticsViewModel.cs` (drop the explicit `Raise(nameof(AutoRefreshHelperLog))` — `Set` already raises; make `IsVisible` notifying via `Set`)
- Create: `src/BlurLink.Shell/Services/ThrowingPlatformServices.cs` (one shared `internal sealed class ThrowingPlatformServices : IPlatformServices` throwing `InvalidOperationException` on all three members); modify both VMs to use it (delete their private duplicates)
- Modify: `src/BlurLink.Shell/ViewModels/JoinSessionViewModel.cs` (preflight preview passes `SelectedAdapter.IfIndex` so display matches the live `ifIdx` filter)
- Modify: `tests/BlurLink.Shell.Tests/UiAudit.cs` (message `Watermark` → `PlaceholderText`)
- Modify: `src/BlurLink.Shell/App.axaml.cs` (hold `TrayIcons` in a field; dispose + unsubscribe on `ApplicationLifetime.Exit` / `MainWindow.Closed`)
- Modify: `src/BlurLink.Shell/Views/DialogWindow.axaml` (add `KeyBinding Gesture="Escape" Command="{Binding CloseCommand}"` — needs a `CloseCommand` on DialogWindow code-behind or VM; simplest: code-behind `public RelayCommand CloseCommand { get; }` calling `Close()`, set `DataContext = this` ONLY for that button? No — cleaner: `KeyBinding` with `Command="{Binding $parent[Window].CloseCommand}"`? Avalonia `$parent` in KeyBinding context is fragile. Chosen shape: code-behind adds the binding in ctor: `KeyBindings.Add(new KeyBinding { Gesture = new KeyGesture(Key.Escape), Command = new RelayCommand(_ => Close()) })` using existing `ShellViewModelBase.RelayCommand`. Esc closes, Enter still hits focused OK.)
- Modify: `src/BlurLink.Platform/HelperIpc.cs` (derive `EmbeddedHelperResource` from the active `_resourceFiles` map, e.g. first value's key, instead of the hardcoded Desktop literal) + Test: extend `tests/BlurLink.Core.Tests/HelperEmbedTests.cs` with `HasEmbedded_FollowsActiveMap` (configure Platform map in-test, assert true; restore default after)
- Modify: `TODO.md`, `docs/REPO.md`, `THIRD-PARTY-NOTICES.md`, `scripts/build-portable.ps1` header comment (`.NET 8` → `.NET 10` prose only, message text already fixed)
- Test: full suite green; name every touched test file in the commit message.

**Interfaces:**
- Consumes: M3 residual ledger lines (all listed above, nothing else).
- Produces: zero deferred hygiene; R1 recorded in REPO.md.

- [ ] **Step 1: Namespace + mojibake + audit message (mechanical)**
Run: `git grep -n 'namespace BlurLink.Core.Tests' -- tests/BlurLink.Shell.Tests` → fix every hit to `namespace BlurLink.Shell.Tests;`. Run: byte-scan Shell sources for `E2 80 94` mis-rendered comments (read raw bytes, replace with UTF-8 `—`). `UiAudit.cs`: message string only.
- [ ] **Step 2: VM one-liners (double-Raise, IsVisible, Throwing dedup, preflight ifIdx)**
Each as specified above; `ThrowingPlatformServices` shared class keeps throw-messages identical to the deleted duplicates (grep the messages first, copy verbatim).
- [ ] **Step 3: Tray dispose + Dialog Esc + HasEmbeddedHelper (+ test)**
As specified above. The embed test must restore the default map in `finally` so later tests are unaffected.
- [ ] **Step 4: Stale prose sweep** — `.NET 8` → `.NET 10` in the four listed spots only (prose; user-facing strings already fixed).
- [ ] **Step 5: Gates** — build 0/0, full suite green, both checkers effectively green (replicate exact skip/scan logic; PS7 absent here), R1 note added to `docs/REPO.md`.
- [ ] **Step 6: Commit** — `git add` only listed files; message names every touched test file + `R1 recorded`.

---

# Stage B — Prove it headed + rewrite the README

## Task 2: Headed verification checklist (performed, not written)

No files. Evidence goes in the Task 3 or Task 5 commit message (whichever runs second — do NOT commit an empty checklist file).

- [ ] **Step 1: Tray** — launch unelevated: icon visible, tooltip mirrors story (`BlurLink — idle` → `Bridge running`), menu Show + Copy diagnostics + Quit all work, no crash on Quit (dispose path).
- [ ] **Step 2: Theme attach** — every destination renders styled (no raw unstyled controls): Join, Settings, Diagnostics, FirstRun overlay.
- [ ] **Step 3: Dialog Esc** — trigger a dialog (e.g. second-instance message or helper error), press Esc, closes. Press Enter on focused OK, closes.
- [ ] **Step 4: First-run overlay + bridge flow** — fresh config (backup settings first, restore after with hash verify): overlay appears, Esc skips, Join connects (or validation gates correctly without overlay — record which, no live Blur required).
- [ ] **Step 5: Record** — window title, build (0.3.0), per-item pass/fail lines for the commit message.

## Task 3: README rewritten around verified-vs-open

**Files:**
- Modify: `README.md` (rewrite); touch up `docs/user-guide.md` ONLY where the rewrite exposes a contradiction (quote both lines in the report).

Required sections, in order: (1) what it is + what it is not (2 lines each, existing copy mostly holds); (2) **Verified** — join-only lobby+join over tunnel (author pair, 2026-09-15, `captured=2 forwarded=1`, no host mode), discovery 50001 fixed by capture + live proof, driver-level suites green; (3) **Still open** — (b) endpoint-dial question (M1 skipped, non-blocking), driver CI PENDING, elevated e2e assertions pending, tray headed-eyeball done Task 2; (4) Install — slim exe + .NET 10 Desktop Runtime link + WinDivert staging + SmartScreen unsigned warning; (5) Use — host IP + adapter + Join, story banner, Advanced for the rest; (6) Host fallback Advanced note (when: strict NAT/mobile/`.255`); (7) Size table (slim only + legacy Full/Sfx kept per R2 with one-line reason); (8) Support — bundle + logs paths. Keep it under 150 lines; cut V1 research narrative to 3 lines pointing at `docs/packet-research.md`.

- [ ] **Step 1: Write the rewrite** (all eight sections, every open item labeled open).
- [ ] **Step 2: Contradiction sweep** — grep user-guide/troubleshooting for anything the new README contradicts; fix the smaller side, quote both.
- [ ] **Step 3: Checkers green** (replicate logic; PS7 absent) + commit with `README: ...` message.

---

# Stage C — Cut the release

## Task 4: Remove WPF from the solution

**Files:**
- Modify: `BlurLink.sln` (`dotnet sln remove src/BlurLink.Desktop/BlurLink.Desktop.csproj`), delete `src/BlurLink.Desktop/` (entire directory), `scripts/build-portable.ps1` (Desktop paths → Shell paths: project, output name stays `BlurLink.exe`, `Native/` staging dir moves to Shell project), `scripts/package-release.ps1` (same retarget; version gate unchanged), `src/BlurLink.Shell/BlurLink.Shell.csproj` (add conditional `EmbeddedResource` items for `Native\blurlink-net.exe` + WinDivert files, mirroring the deleted Desktop stanza verbatim except the resource prefix, which is already `BlurLink.Shell.Native.*` in `ShellEmbeddedFiles`), `docs/REPO.md` (one paragraph: WPF removed at 0.3.0, Shell is the app, Desktop history retained in git).

**Interfaces:**
- Consumes: Task 1 (HasEmbeddedHelper derivation makes the Shell embed check real).
- Produces: Shell-only solution; portable pipeline publishing the Shell.

- [ ] **Step 1: Read `scripts/build-portable.ps1` fully first** (it has per-flavor paths + the g++ glob + MSVC path — retarget each Desktop reference to Shell, nothing else).
- [ ] **Step 2: Remove + retarget + embeds** as listed. `git rm -r src/BlurLink.Desktop` (not plain delete — keep history explicit).
- [ ] **Step 3: Prove `Desktop.Services` is fully gone**: `git grep -rn 'Desktop.Services\|BlurLink.Desktop' -- src tests scripts | grep -v '.superpowers/'` must print nothing except historical docs + REPO.md note. Fix every hit or record why it stays.
- [ ] **Step 4: Gates** — build 0/0, full suite green, checkers green, portable build runs (`build-portable.ps1` default slim) producing a launching Shell exe.
- [ ] **Step 5: Commit** — message records embed proof (helper staged from Shell resources), the grep proof, and R1 pointer.

## Task 5: Release cut 0.3.0 + D12 record

**Files:**
- Modify: `docs/aot-spike.md` (append D12 verdict section — do not rewrite existing rows).

- [ ] **Step 1: Dry run** — `pwsh scripts/package-release.ps1 -Version 0.3.0` (needs PS7: run in CI if absent here — do NOT fake it; if unrunnable, record PENDING with the exact blocker and prove the gate logic instead via FileVersion + build, as Task D did). Verify: zip exists, 4 entries (exe + LICENSE + notices + SHA256SUMS), hashes re-verify, exe launches + stages helper.
- [ ] **Step 2: D12 record** — append: AOT failed both shells → `-Full`/`-Sfx` RETAINED, one paragraph, with the NETSDK1168 + IL2026/IL3050 pointers. This closes spec Q7.
- [ ] **Step 3: Tag (user-confirmed first)** — `git tag v0.3.0`, push tag to origin ONLY after explicit user approval in-session (side effect). Record the approval line in the report.
- [ ] **Step 4: Commit** — D12 section + dry-run evidence (or PENDING with blocker + gate-logic proof).

## Task 6: Mirror runbook (doc-only; executes at mirror time)

**Files:**
- Modify: `docs/REPO.md` (append `## Public mirror runbook`).

Content (exact commands, R3 method): fresh clone → `git checkout -b public a238ad0` → `git log --oneline | head` proof → banned-map grep proof (`git grep` for each banned literal, expect zero outside the checker) → `git remote add github <url>` → `git push github public:main` → trigger `driver.yml` workflow_dispatch → run release `workflow_dispatch(version: 0.3.0)` → fill `docs/driver-ci-experiment.md` runner + verbatim lines + consequence branch. Explicit: never push full history public; never run this before the user provides the GitHub URL.

- [ ] **Step 1: Write the runbook** (commands copy-pasteable, each with expected output).
- [ ] **Step 2: Checkers green + commit** (`docs: ...` message).

## Task 7: Port the 36 deleted behavior tests to Shell.Tests (tracked coverage restoration)

Added by controller ruling after the Task 4 review (wrongful-deletion finding): 41 behavior tests were deleted with the WPF VMs; 4 were moved and 1 Platform test restored, leaving 36 that exercise Contracts/Platform/Core behavior the Shell VMs claim as verbatim ports. They must live on as Shell headless tests (or be individually justified as unportable), not vanish silently. Runs BEFORE Task 5's release cut.

**Files:**
- Create: `tests/BlurLink.Shell.Tests/WpfParityPortTests.cs` (one home for all ports; namespace `BlurLink.Shell.Tests`; `[AvaloniaFact]` where a view renders, `[Fact]`(v3) for pure VM logic — check how Shell.Tests fact attributes are used first and follow the file-local pattern)
- Source material (retrieve each from git, `git show 495c26d:<path>` — never rewrite from memory): `HostViewModelTests.cs` (14 status-framing tests → Shell `HostViewModel`), `JoinStoryWiringTests.cs` (2 story/counters → Shell Join facade), `JoinViewModelBusyStateTests.cs` (5 → Shell Join session part), `JoinViewModelStartFlowTests.cs` (5 → Shell Join session part), `ReplyListenFailureTests.cs` (3 → Shell DiscoverySniff part), `SettingsImportResetTests.cs` (5 → Shell SettingsVM), `MainViewModelTests.cs` (2 dropped → Shell MainViewModel)
- Each port: same assertions against the Shell counterpart (`ForTests` seams exist on Join/Host VMs per M3 Tasks 4–5; follow those patterns). If a test is genuinely unportable (STA XAML-render of deleted WPF views, WPF-only API), do NOT port it — list it with the exact reason instead. Unported-with-reason is honest; silently dropped is forbidden.

**Interfaces:**
- Consumes: M3 Tasks 4–6 seams; Task 4's moved-test patterns.
- Produces: suite with the 36 behaviors covered again (or reasoned non-ports); release cut unblocked.

- [ ] **Step 1: Retrieve + map** — list all 36 (class + name + Shell target) in the report before writing code.
- [ ] **Step 2: Port** — same bodies, Shell targets, v3 attributes, `using Xunit;`.
- [ ] **Step 3: Gates** — build 0/0, full suite green (compute expected: Core stays, Shell grows by ported count), checkers effectively green (replicate; PS7 absent).
- [ ] **Step 4: Commit** — message names the port file + count + any reasoned non-ports.

---

## Verification ledger

| Task | Status | Evidence to record |
|---|---|---|
| 1 | | suite green; every touched test file named; R1 in REPO.md |
| 2 | | 5 checklist lines (tray/theme/dialog/first-run) with build + title |
| 3 | | README ≤150 lines, 8 sections, contradiction sweep quoted |
| 4 | | Desktop.Services grep empty; portable Shell exe launches + stages |
| 5 | | dry-run evidence or PENDING+blocker; D12 section; tag approval line |
| 6 | | runbook commands + expected outputs; checkers green |
| 7 | | 36 behaviors covered again (or reasoned non-ports listed); suite green |

Stage exit criteria: hygiene zero, README honest, WPF gone, portable proven, tag pushed (approved), mirror path documented — M4 done.
